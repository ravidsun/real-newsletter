# Development profile - only overrides for dev-specific settings
spring:
  flyway:
    baselineOnMigrate: false
    cleanDisabled: false

  ai:
    openai:
      api-key: ${OPENAI_API_KEY:sk-test-key-not-real}

external:
  news:
    api:
      key: ${NEWSDATA_API_KEY:test-key}


